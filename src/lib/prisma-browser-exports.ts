/**
 * Browser-safe Prisma re-exports (enums, types, Prisma namespace).
 * NO PrismaClient — use `@/lib/prisma-exports` for server-side code.
 */
export * from '../../prisma/generated/prisma/browser';
